import React from "react";
import { useState } from "react";
type Props = {};

const RUserList: React.FC<Props> = ({}) => {
  return (
    <div
      style={{ backgroundColor: "lightgray", width: "100vw", height: "100vh" }}
    >
      RUser List
    </div>
  );
};

export default RUserList;
